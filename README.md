Example how to setup TestContainers (https://www.testcontainers.org) in tests.

1. Run one of the default containers
2. Run container based on custom docker image
3. Run container based on custom docker-compose